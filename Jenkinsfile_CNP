#!groovy

properties([
  [
    $class       : 'GithubProjectProperty',
    displayName  : 'Evidence Management Viewer',
    projectUrlStr: 'https://github.com/hmcts/em-viewer-web/'
  ],
  pipelineTriggers([
    [$class: 'GitHubPushTrigger']
  ])
])

@Library("Infrastructure") _

def type = "nodejs"
def product = "em"
def component = "viewer-web"

def channel = '#dm-pipeline'

withPipeline(type , product, component) {
    after('checkout') {
        echo '${component} out'
    }

    after('build') {
      sh 'yarn setup'
    }

    enableSlackNotifications(channel)
}
